[project]
name = "raw-kv-store"
version = "0.1.0"
description = "A simple socket-based key-value store with get and set operations"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "msgpack>=1.0.0",
]
